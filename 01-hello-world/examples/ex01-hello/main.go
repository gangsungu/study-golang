// 패키지 선언. 이 코드가 어떤 패키지에 속하는지 알려준다.
// Go의 모든 코드는 반드시 패키지 선언으로 시작해야 한다.
// main 패키지는 Go 프로그램의 진입점이 되는 패키지로, main 함수를 포함해야 한다.
package main

// fmt: 표준 입출력을 담당하는 내장 패키지
import "fmt"

// main 함수는 프로그램 실행 시 가장 먼저 호출되는 진입점이다.
// 매개변수도 반환값도 가질 수 없다.
func main() {
	fmt.Println("Hello, World!")
}
